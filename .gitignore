public/
resources/
.hugo_build.lock

# leftover from a failed `git submodule add` attempt in a sandboxed
# environment (FUSE permission quirk prevented cleanup) — safe to
# delete manually via File Explorer whenever convenient.
themes/_congo_broken_*/
